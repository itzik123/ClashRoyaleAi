"""Measurement harnesses. Every file here exists because a claim needed
settling; none of them is imported by the training path.

`prove_*` are engine-scored (the engine is the oracle), `probe_*` read a policy's
behaviour, `*_ab` are paired A/B comparisons."""
