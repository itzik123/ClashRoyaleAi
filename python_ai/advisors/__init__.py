"""The deterministic tactical layer: engine-validated placement rules
(`tactics`), those rules turned into a training target for the placement head
(`advisor_target`), and the inference-time policy that composes them with the
network (`hybrid_policy`)."""
