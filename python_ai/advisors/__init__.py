"""The deterministic tactical layer: engine-validated placement rules (`tactics`),
those rules as a training target for the placement head (`advisor_target`), and
the inference-time composition with the network (`hybrid_policy`).
"""
